import { StyleSheet } from "react-native";

export const selectGuestStyles = StyleSheet.create({
  appBar: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
  },
  header: {
    fontSize: 18,
    fontWeight: "500",
    marginLeft: 50,
  },
});
