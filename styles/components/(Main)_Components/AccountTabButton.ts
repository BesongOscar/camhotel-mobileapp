import { StyleSheet } from "react-native";
export const accountTabButtonStyles = StyleSheet.create({
  buttonContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingVertical: 10,
    width: "100%",
  },
  labelContainer: {
    flexDirection: "row",
    alignItems: "center",
    gap: 17,
  },
  title: {
    fontWeight: "600",
  },
});
