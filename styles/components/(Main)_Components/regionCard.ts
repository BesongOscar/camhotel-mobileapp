import { StyleSheet } from "react-native";
export const regionCardStyles = StyleSheet.create({
  CardContainer: {
    alignItems: "flex-start",
  },
  imageholder: {
    marginBottom: 10,
    borderRadius: 12,
    height: 80,
    width: 90,
    backgroundColor: "grey",
    marginHorizontal: 5,
  },
});
