import { StyleSheet } from "react-native";
export const appBarStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 100
  },
  appBarTitle: {
    fontSize: 18,
    fontWeight: "400",
    letterSpacing: 0.5,
  },
});
